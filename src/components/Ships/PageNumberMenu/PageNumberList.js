// import PageNumberItem from "./PageNumberItem";
// import classes from "./PageNumberList.module.css";

//WIP: Pagination

// const PageNumberList = ({ changePageHandler, totalPages }) => {
//   // let array = [];
//   // let i = 0;
//   // do {
//   //   i = i + 1;
//   //   array.push(i);
//   // } while (i < totalPages);
//   const content = Array.from(Array(totalPages)).map((_, i) => (
//     <PageNumberItem
//       number={i}
//       key={i}
//       onClick={() => changePageHandler(i + 1)}
//     />
//   ));

//   // Array.from(Array(3)).forEach()

//   return <ul className={classes.pageNumberList}>{content}</ul>;
// };
// export default PageNumberList;
